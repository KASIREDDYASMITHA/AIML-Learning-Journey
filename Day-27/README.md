# AIML Learning Journey - Day 27

## Topics Covered

Today I learned important Pandas concepts related to data inspection, filtering, cleaning, grouping, dates, and categorical data.

### Concepts Learned

- `df.shape`
- `df.dtypes`
- `df.describe()`
- `df.head()`
- `df.loc[]`
- Filtering rows using conditions
- `df.drop()`
- `df.replace()`
- `df.groupby()`
- Date range filtering
- Handling invalid values
- Checking missing values using `isnull().sum()`
- Cleaning latitude and longitude values
- Categorical data
- One-hot encoding using `pd.get_dummies()`

